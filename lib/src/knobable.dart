import 'package:flutter/widgets.dart';

import 'property.dart';

class Knobable extends StatelessWidget {
  const Knobable({
    super.key,
    required this.properties,
  });

  final List<Property> properties;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
